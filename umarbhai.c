#include <stdio.h>
#include <conio.h>

void main()
{
    /*
    
    int age;
    printf("app age likhen \n");
    scanf("%d", &age);

    if (age==18 || age==19)
    {
        printf("you can drive but care fully %d", age);
    }else if(age >18 && age < 60){
          printf("you can drive %d", age);

    }else if(age <18)
    {
          printf("beta app choty hen %d", age);
    }else
    {
          printf("you are old so take care and rest in home %d", age);
    }

    */


}
